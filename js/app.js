const c=document.getElementById('stars'),x=c.getContext('2d');let stars=[];function size(){c.width=innerWidth;c.height=innerHeight;stars=Array.from({length:Math.min(180,Math.floor(innerWidth/7))},()=>({x:Math.random()*c.width,y:Math.random()*c.height,z:Math.random()*1.8+.2}))}function draw(){x.clearRect(0,0,c.width,c.height);for(const s of stars){s.y+=s.z*.28;if(s.y>c.height)s.y=0;x.fillStyle=`rgba(120,230,255,${.15+s.z*.22})`;x.fillRect(s.x,s.y,s.z,s.z)}requestAnimationFrame(draw)}size();draw();addEventListener('resize',size);function tick(){document.getElementById('clock').textContent=new Date().toLocaleTimeString('he-IL');document.getElementById('net').textContent=(10+Math.random()*8).toFixed(1)+' Gb/s';document.getElementById('ai').textContent=(96+Math.random()*3.7).toFixed(1)+'%'}setInterval(tick,1200);tick();function ignite(){ai.textContent='100%';net.textContent='42.0 Gb/s';sec.textContent='MAXIMUM';threat.textContent='SCANNING';cmd.innerHTML='<span class="ok">SYSTEM IGNITED — WELCOME, ITAMAR</span>';toast.classList.add('show');setTimeout(()=>{toast.classList.remove('show');threat.textContent='LOW'},2200);burst()}function chaos(){document.documentElement.style.setProperty('--cyan','#ff3cac');document.documentElement.style.setProperty('--violet','#36f5ff');cmd.textContent='CHAOS VISUAL PROTOCOL ENABLED';document.querySelector('.aurora').style.animationDuration='2.5s';burst();setTimeout(()=>document.querySelector('.aurora').style.animationDuration='18s',5000)}function burst(){for(let i=0;i<45;i++){let p=document.createElement('i');Object.assign(p.style,{position:'fixed',zIndex:25,left:'50%',top:'48%',width:'3px',height:'3px',borderRadius:'50%',background:'var(--cyan)',boxShadow:'0 0 12px var(--cyan)',pointerEvents:'none'});document.body.appendChild(p);let a=Math.random()*Math.PI*2,d=80+Math.random()*420;p.animate([{transform:'translate(0,0)',opacity:1},{transform:`translate(${Math.cos(a)*d}px,${Math.sin(a)*d}px)`,opacity:0}],{duration:700+Math.random()*900}).onfinish=()=>p.remove()}}document.querySelectorAll('.tilt').forEach(el=>{el.addEventListener('mousemove',e=>{let r=el.getBoundingClientRect(),rx=(e.clientY-r.top-r.height/2)/18,ry=-(e.clientX-r.left-r.width/2)/18;el.style.transform=`perspective(700px) rotateX(${rx}deg) rotateY(${ry}deg) translateY(-6px)`});el.addEventListener('mouseleave',()=>el.style.transform='')});addEventListener('mousemove',e=>{document.querySelector('.aurora').style.transform=`translate(${(e.clientX-innerWidth/2)*.015}px,${(e.clientY-innerHeight/2)*.015}px)`});